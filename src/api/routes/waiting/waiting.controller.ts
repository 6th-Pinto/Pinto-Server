export const sameSchooelSameMajorWaitingLine = [];
export const sameSchooleDiffMajorWaitingLine = [];
export const diffSchooleWaitingLine = [];
/*

export const making2Room =  (req: Request, res: Response) => {
    try {

        for (let i = 0; i < sameSchooelSameMajorWaitingLine.length; i++) {
            
            
        }
        res.json();
    }
    catch (e) {
        console.log(e);
    }
}

export const making4Room =  (req: Request, res: Response) => {
    try {
         // 1. 대기리스트에 있는 사람들을 방에 집어 넣음
         const createRoomName = Math.random().toString(36);

        // 2. 방에 넣은 사람들은 대기리스ㅡ에서 삭제
        return
    } catch(e) { 
        console.log(e)
    }
}


export const makingRoomName = (req: Request, res: Response) => {
    // 랜덤 방 이름 생성
    const createRoomName = Math.random().toString(36);
    return createRoomName;
    
}
*/